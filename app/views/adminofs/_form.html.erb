<%= form_with(model: adminof, local: true) do |form| %>
  <% if adminof.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(adminof.errors.count, "error") %> prohibited this adminof from being saved:</h2>

      <ul>
        <% adminof.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :course_id %>
    <%= form.text_field :course_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
